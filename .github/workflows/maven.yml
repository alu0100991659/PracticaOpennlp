name: Java CI

on: [push]

jobs:
  build:

    runs-on: windows

    steps:
    - uses: actions/checkout@v1cd %Java
    - name: Set up JDK 11.0.4
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn compile --file pom.xml
